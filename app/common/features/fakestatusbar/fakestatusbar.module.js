/* global angular*/
(function () {
  'use strict';
  /**
   * @ngdoc overview
   * @name fakestatusbar
   * @description
   * <h1>Fake Status Bar</h1>
   <p>This is a directive for the fake status bar which takes two arguments as parameter for
   the images and a element id.</p>
   */
  angular
  .module('feature.fakestatusbar', [
    // Inject dependencies here
  ]);
})();
