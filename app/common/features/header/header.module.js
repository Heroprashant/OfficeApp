/* global angular*/
(function () {
  'use strict';
  /**
   * @ngdoc overview
   * @name header
   * @description
   * <h1>Header</h1>
   <p>This is a directive for the header which takes two arguments as parameter for
   the images and a element id.</p>
   */
  angular
  .module('feature.header', [
    // Inject dependencies here
    'ionic'
  ]);
})();
