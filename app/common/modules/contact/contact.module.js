/* global angular*/
(function() {
    'use strict';
    /**
     * @ngdoc overview
     * @name contact
     * @description
     * <h1>Contact</h1>
     <p>This is a directive for the information modal which takes two arguments as parameter for
     the images and a element id.</p>
     */
    angular
        .module('module.contact', [
            // Inject dependencies here
            'ionic'
        ]);
})();
